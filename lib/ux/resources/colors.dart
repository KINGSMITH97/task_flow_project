import 'package:flutter/material.dart';

class ColorSource {
  /// all colors to be used in the UI are defined here for easy access and
  /// update if necessary
  static const Color black = Color(0xFF000000);
  static const Color white = Color(0xFFFFFFFF);
}
